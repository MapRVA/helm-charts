apiVersion: v1
kind: Service
metadata:
  name: subjects-oxigraph
  namespace: {{ .Release.Namespace }}
  labels:
    app: subjects-oxigraph
spec:
  type: ClusterIP
  selector:
    app: subjects-oxigraph
  ports:
    - name: http
      port: 7878
      targetPort: 7878
      protocol: TCP
